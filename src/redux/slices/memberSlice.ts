import {createSlice} from '@reduxjs/toolkit';

interface State {

};

const initialState: State = {

};

export const memberSlice = createSlice({
  name: 'member',
  initialState,
  reducers: {

  },
});
